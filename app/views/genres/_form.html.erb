<%= form_with(model: genre) do |form| %>
  <% if genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(genre.errors.count, "error") %> prohibited this genre from being saved:</h2>

      <ul>
      <% genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <% if genre.persisted? %>
      <div class="float-end">
        <%= link_to 'Destroy', genre, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if genre.persisted? %>
      <%= link_to "Cancel", genre, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", genres_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
