<%= form_with(model: actor) do |form| %>
  <% if actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor.errors.count, "error") %> prohibited this actor from being saved:</h2>

      <ul>
      <% actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <% if actor.persisted? %>
      <div class="float-end">
        <%= link_to 'Destroy', actor, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if actor.persisted? %>
      <%= link_to "Cancel", actor, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", actors_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
