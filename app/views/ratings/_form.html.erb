<%= form_with(model: rating) do |form| %>
  <% if rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
      <% rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :value, class: 'form-label' %>
      <%= form.text_field :value, class: 'form-control' %>
    </div>

  <div class="mb-3">
    <%= form.label :movie_id, class: 'form-label' %>
    <%= form.select :movie_id, options_from_collection_for_select(Movie.all, 'id', 'title', selected: rating.movie_id), class: 'form-control' %>
    </div>

  <div class="mb-3">
    <% if rating.persisted? %>
      <div class="float-end">
        <%= link_to 'Destroy', rating, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if rating.persisted? %>
      <%= link_to "Cancel", rating, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", ratings_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
